http {
    server {
        listen       80;
        listen       80;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location {
            root   html;
            index  index.html index.htm;
            method GET POST PUT;
        }
    }
}
